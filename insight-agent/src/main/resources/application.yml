server:
  port: 8123
  servlet:
    context-path: /api

spring:
  application:
    name: insight-agent

  ai:
    deepseek:
      # Spring AI auto-config requires hasText, so default to a placeholder.
      # Real calls will 401 until you `setx DEEPSEEK_API_KEY sk-...` and reopen shell.
      api-key: ${DEEPSEEK_API_KEY:placeholder-set-DEEPSEEK_API_KEY-env-var}
      chat:
        options:
          model: deepseek-chat
          temperature: 0.3
          max-tokens: 4096

    ollama:
      base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
      chat:
        options:
          model: gemma3:1b
      embedding:
        options:
          model: bge-m3

    # PGVector dimensions MUST be 1024 for BGE-M3.
    # Enabled later when PostgreSQL+pgvector is provisioned.
    # vectorstore:
    #   pgvector:
    #     dimensions: 1024
    #     distance-type: COSINE_DISTANCE
    #     index-type: HNSW

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: default
      paths-to-match: /**
      packages-to-scan: com.insightagent.controller

knife4j:
  enable: true
  setting:
    language: zh_cn

# InsightAgent app-specific config
app:
  memory:
    # Where FileChatMemoryRepository writes one .kryo file per conversation.
    # Override per env via APP_MEMORY_DIR. Default sits next to the running JAR.
    dir: ${APP_MEMORY_DIR:${user.dir}/chat-memory}
